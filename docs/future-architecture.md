# Deferred work

- Website enquiry routing after the separate website review and an agreed mapping.
- Audited import of selected spreadsheet records, only with explicit approval; never store customer records in source/seed files.
- Direct email, Calendar, invoicing, accounting and payments after the core model is proven.
- Granular typed time fields, payment ledger and detailed rehearsal scheduling if usage needs them.
- Private Storage uploads for music/contracts. V1 uses private links; no bucket is needed.
- Password self-service recovery/email configuration. V1 recovery is through the Supabase administrator.
- Realtime list refresh, catalogue revision checks and archive pagination as data grows. V1 loads the small two-owner workspace in one request.
- No portals, generic workflow builder, custom-field engine, bank integration or multi-business SaaS in V1.
