- Dashboard launch
  - Built: enquiry/event editing, derived workflow, action queues, deferred details, musicians, repertoire, finance, settings, private sign-in and database integration.
  - Public website and original archives remain unchanged. Live customer data has not been imported.
  - The live database, owner accounts and Vercel environment are not configured yet.
  - [ ] Approve the proposed launch setup and defaults, or list only the changes you want.
    - Separate **Horizon Strings Operations** Supabase project in Sydney and a separate Vercel project, both under **Golden Thread Solutions**.
    - Two owner email/password accounts; start on the generated Vercel address. A custom domain can wait.
    - Editable timing defaults: musicians/internal preparation 12 weeks, repertoire 6, arrangements 4, rehearsal 3, final details 2, final confirmation/balance 1; post-event actions 7 days after.
    - Event Ready requires resolved details, confirmed musicians with music, approved repertoire, final confirmation, completed pre-event tasks and full client payment. The payment requirement can be switched off in Settings.
  - Supabase setup — [open Supabase](https://supabase.com/dashboard)
    - [ ] Create/select the dashboard project and provide its **Project URL** and **publishable key**. Do not provide a secret/service-role key or database password.
    - [ ] Run the prepared [dashboard migration](../supabase/migrations/202609180001_dashboard_v1.sql) once in SQL Editor.
    - [ ] Create the two confirmed email/password users in Authentication → Users, and disable new-user signup.
      - Which two email addresses should have owner access? Confirm whether the first is `koby@horizonstrings.com.au` and supply the second.
      - Keep passwords private. Directly creating users avoids needing invitation email/SMTP setup for this first launch.
      - Run [configure-owners.sql](../supabase/configure-owners.sql) after replacing its two email placeholders. No user IDs need to be copied manually.
    - [ ] Confirm a backup/restore arrangement before entering real customer data. Settings also provides a private JSON backup export.
  - Vercel setup — [open Vercel](https://vercel.com/new)
    - [ ] Import `golden-thread-solutions/horizon-strings-internal-dashboard` into Golden Thread Solutions; choose Next.js, root `.`, Node 24.
      - Add `NEXT_PUBLIC_SUPABASE_URL` and `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` using the values above, then deploy the approved release.
      - Keep the website project separate. No Resend, Google, bank, Storage-bucket or service-role keys are needed for this dashboard V1.
    - [ ] Return the deployed URL when setup is complete; Codex can finish the two-owner persistence/access checks and release handoff.
  - Final operating check
    - [ ] After live smoke tests pass, try one synthetic event and confirm the saved Settings defaults work for your process before entering real clients.
    - Existing spreadsheet import and website-to-dashboard enquiry routing remain separate future work.
